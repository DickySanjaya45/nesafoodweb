// ...new file...
import 'package:flutter/material.dart';

class NesaColors {
  // Soft terracotta primary
  static const Color terracotta = Color(0xFFDD6A3A); // softer than previous
  // Very light terracotta used for subtle backgrounds / accents
  static const Color terracottaLight = Color(0xFFF6E8DD);
  // App background
  static const Color background = Color(0xFFF7F5F3);
}
